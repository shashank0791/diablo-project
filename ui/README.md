diablo-project UI
=================

Development:
------------
* `npm install`
* `npm start`

Dist package:
-------------
* Run: `npm run dist`
* the `./dist` directory will have the necessary files on it