diablo-project UI
=================

Development:
------------
* `npm install`
* `npm start`

Dist package:
-------------
* the `./dist` contains the latest working version
* To generate a new package run: `npm run dist`